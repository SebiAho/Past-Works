#pragma once
//Holds enums used by the system

enum class Comp { DEFAULT = 0, TRANS, REND, PHYS, INPUT, TEXT, COLL };//Combonent id

enum class Sys{ DEFAULT = 0, GRAPHIC, PHYSICS, INPUT };//System id